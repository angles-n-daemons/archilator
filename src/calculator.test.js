const Calculator = require('./calculator.js');

describe('Calculator', function() {
    it('is a test so jest doesnt complain', function() {

    });
    // tests to be added when the project is complete
});
